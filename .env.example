# Local dev only (npm run dev). Copy this file to .env and adjust as needed.
# Leave unset for the production Docker build — nginx proxies /api/* to the api container instead.
VITE_API_BASE_URL=http://localhost:8080
